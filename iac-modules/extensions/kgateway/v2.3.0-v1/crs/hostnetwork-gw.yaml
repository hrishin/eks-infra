apiVersion: gateway.kgateway.dev/v1alpha1
kind: GatewayParameters
metadata:
  name: hostnetwork-gw-params
  namespace: kgateway-system
spec:
  kube:
    service:
      type: ClusterIP
    deployment:
      replicas: 1
    deploymentOverlay:
      spec:
        template:
          spec:
            hostNetwork: true
            securityContext:
              sysctls: []
            containers:
              - name: kgateway-proxy
                securityContext:
                  allowPrivilegeEscalation: true   # needed to gain capabilities
            dnsPolicy: ClusterFirstWithHostNet
            nodeSelector:
              node-type: core
            tolerations:
              - key: node-type
                operator: Equal
                value: core
                effect: NoSchedule
              - key: node.cilium.io/agent-not-ready
                operator: Exists
                effect: NoSchedule
---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: kgateway-hostnetwork
spec:
  controllerName: kgateway.dev/kgateway
  parametersRef:
    group: gateway.kgateway.dev
    kind: GatewayParameters
    name: hostnetwork-gw-params
    namespace: kgateway-system
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: main-gateway
  namespace: kgateway-system
spec:
  gatewayClassName: kgateway-hostnetwork
  listeners:
  - name: http
    protocol: HTTP
    port: 3080
  - name: https
    protocol: HTTPS
    port: 30443
    tls:
      mode: Terminate
      certificateRefs:
        - name: main-gateway-tls
          namespace: kgateway-system
